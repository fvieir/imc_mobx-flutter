name: mobx_imc
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1
environment:
 sdk: ">=2.18.0-255.2.beta <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  mobx: ^2.0.3
  flutter_mobx: ^2.0.1
  cupertino_icons: ^1.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  mobx_codegen: ^2.0.2
  build_runner: ^2.1.0
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
